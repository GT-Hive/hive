import 'rxjs';

import { actionCreators } from '../actions/navigation';
import { actionTypes } from '../actions/user';

// export default action$ => action$
//   .ofType(actionTypes.REGISTER_USER)
//   // .switchMap(action => api.registerUser(action.interests, action.userInfo)
//   .map(() => actionCreators.resetStack());
