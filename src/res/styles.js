const STYLES = {
  TEXT_PRIMARY: {
    fontSize: 35,
    lineHeight: 40
  },
  TEXT_SECONDARY: {
    fontSize: 23,
    lineHeight: 30
  },
  TEXT_TERTIARY: {
    fontSize: 16,
    lineHeight: 20
  },
  // TODO(roy): PROFILE_IMG should be the style for the users once inside the app
  PROFILE_IMG: {
    height: 60,
    borderRadius: 50,
    width: 60
  }
};

export default STYLES;
